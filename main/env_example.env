# 数据目录配置
DATA_DIR=./data
OUTPUT_DIR=visualizations
CHECKPOINT_DIR=checkpoints

# 训练参数
BATCH_SIZE=256
NUM_WORKERS=2
EPOCHS=200
LEARNING_RATE=0.1
MOMENTUM=0.9
WEIGHT_DECAY=5e-4
SEED=42

# 可视化参数
MAX_TSNE_POINTS=5000
GRADCAM_SAMPLES=8
RELIABILITY_BINS=10

# 设备配置
DEVICE=cuda  # 可选值: cuda 或 cpu
AMP=True  # 是否使用自动混合精度训练

# 检查点路径
BEST_CHECKPOINT=checkpoints/best.pth
LAST_CHECKPOINT=checkpoints/last.pth
HISTORY_FILE=checkpoints/history.json